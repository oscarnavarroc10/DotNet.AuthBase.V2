# Source Code

This directory will contain the application source code for DotNet.AuthBase.V2.

> **Status:** Not yet implemented. This directory is a placeholder.
>
> This initial pull request contains documentation and repository structure only. No application code has been written.

## Planned Structure

```
src/
├── DotNet.AuthBase.Core/           # Domain models, interfaces, abstractions
├── DotNet.AuthBase.Infrastructure/ # Data access, token storage, integrations
└── DotNet.AuthBase.Api/            # HTTP endpoints and middleware
```

## Prerequisites for Implementation

Before any source code is added to this directory:

1. All relevant feature specifications in `specs/` must be in `Approved` status.
2. Architecture decisions must be recorded in `docs/adr/`.
3. The technology stack must be confirmed in `docs/TECH_STACK.md`.

## Getting Started (Future)

Once implementation begins, build and run instructions will be added here.
